// const somar = numero => console.log(`recebi um ${numero}`)

// somar(2);

// const somar = (numeroA, numeroB) => console.log(`Recebi um número ${numeroA} e um numero ${numeroB}`)

// somar(20, 30);

const somar = (numeroA, numeroB) => {
    console.log(`Recebi um numero ${numeroA} e um número ${numeroB}`)
    return numeroA + numeroB
}

console.log(somar(20,30));